{
  "name": "github-issues",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run --project=github-issues",
    "test:watch": "vitest --project=github-issues",
    "build": "esbuild index.js --bundle --platform=node --target=node24 --format=esm --external:@aws-sdk/* --outfile=.build/index.mjs"
  }
}
