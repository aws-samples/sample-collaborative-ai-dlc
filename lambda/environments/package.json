{
  "name": "environments",
  "private": true,
  "type": "module",
  "main": "index.js",
  "scripts": {
    "test": "vitest run --project=environments",
    "test:watch": "vitest --project=environments",
    "build": "esbuild index.js status.js --bundle --platform=node --target=node24 --format=esm --banner:js=\"import{createRequire}from'node:module';const require=createRequire(import.meta.url);\" --outdir=.build --out-extension:.js=.mjs",
    "lint": "oxlint .",
    "format": "oxfmt .",
    "format:check": "oxfmt --check ."
  },
  "dependencies": {
    "@aws-sdk/client-bedrock-agentcore": "^3.1092.0",
    "@aws-sdk/client-bedrock-agentcore-control": "^3.1092.0",
    "@aws-sdk/client-codebuild": "^3.1092.0",
    "@aws-sdk/client-dynamodb": "^3.1092.0",
    "@aws-sdk/client-ecr": "^3.1092.0",
    "@aws-sdk/client-s3": "^3.1092.0",
    "@aws-sdk/lib-dynamodb": "^3.1092.0"
  }
}
