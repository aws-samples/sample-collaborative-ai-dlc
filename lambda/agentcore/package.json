{
  "name": "agentcore",
  "version": "1.0.0",
  "private": true,
  "description": "Bedrock AgentCore Runtime image: executes one AI-DLC v2 stage per invocation, with a clean MCP business-graph server. ESM; imports the CJS lambda/shared helpers via Node interop.",
  "type": "module",
  "main": "http-server.js",
  "scripts": {
    "test": "vitest run --project=agentcore",
    "lint": "oxlint .",
    "format:check": "oxfmt --check ."
  },
  "dependencies": {
    "@aws-sdk/client-apigatewaymanagementapi": "^3.1060.0",
    "@aws-sdk/client-dynamodb": "^3.1060.0",
    "@aws-sdk/client-lambda": "^3.1066.0",
    "@aws-sdk/client-s3": "^3.1060.0",
    "@aws-sdk/client-ssm": "^3.1060.0",
    "@aws-sdk/credential-providers": "^3.1060.0",
    "@aws-sdk/lib-dynamodb": "^3.1060.0",
    "@modelcontextprotocol/sdk": "^1.29.0",
    "gremlin": "^3.8.1",
    "gremlin-aws-sigv4": "^3.6.1",
    "zod": "^3.23.0"
  }
}
