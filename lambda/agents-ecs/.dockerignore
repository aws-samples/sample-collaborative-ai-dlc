# Keep the build context small and stable so layer caching works.
# These dirs/files are never needed inside the image — package.json + npm install
# inside the Dockerfile fully reconstructs runtime deps.
node_modules
**/node_modules
package-lock.json
**/package-lock.json
.git
.gitignore
test
**/test
*.test.js
.dockerignore
